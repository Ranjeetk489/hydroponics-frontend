import React from 'react';

// TODO: implement login logout & register
// TODO: add the logic for handling the above to this
// TODO: utilize {children} to implement context
// TODO: export the logic as a custom hook

// TODO: custom hook will be utilized like so:
// TODO: const { user, login, logout, register } = useAuth()

const CurrentUserContext = React.createContext();
export default CurrentUserContext;
